# M5 & M6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement 2D-MCA for risk assessment and enforce Olofsson area adjustment in landcover predictions.

**Architecture:** Modify `monte_carlo_risk.rs` to use nested loops for epistemic vs. aleatory uncertainty. Modify `landcover_engine.py` to calculate the confusion matrix and adjusted area, emitting a JSON `ScientificResult`.

**Tech Stack:** Rust 2021, Python 3.

## Global Constraints

- No placeholder logic; exact formulas must be used.
- All code must pass unit tests.
- Maintain backward compatibility where possible but enforce the new scientific standards.

---

### Task 1: M6 - 2D Monte Carlo Analysis (2D-MCA)

**Files:**
- Modify: `src/tools/calculators/monte_carlo_risk.rs`

**Interfaces:**
- Produces: `simulate(...)` returning a String but internally calculating 2D arrays.

- [ ] **Step 1: Write the failing test**
- [ ] **Step 2: Run test to verify it fails**
- [ ] **Step 3: Write minimal implementation (nested loops)**
- [ ] **Step 4: Run test to verify it passes**
- [ ] **Step 5: Commit**

### Task 2: M5 - Landcover Engine Olofsson Integration

**Files:**
- Modify: `src/tools/gis/landcover_engine.py`
- Modify: `src/server.rs` (to call `landcover_engine.py` with `--json-result` and parse it)

**Interfaces:**
- Produces: Python script emitting JSON representing a `ScientificResult`.

- [ ] **Step 1: Write the failing test (Python)**
- [ ] **Step 2: Run test to verify it fails**
- [ ] **Step 3: Write minimal implementation (Python)**
- [ ] **Step 4: Run test to verify it passes**
- [ ] **Step 5: Commit**
